name: Build & deploy Web App + APIM

on:
  push:
    branches: [ master ]
    paths: [ 'Azure_File_Ingestion/**' ]
  workflow_dispatch:

env:
  RG: rg_fileingest
  APIM: apim-fileingest
  WEBAPP: webapp-fileingest
  API_ID: fileapi
  API_PATH: file
  SERVICE_URL: https://webapp-fileingest-bjdqctc5cxgpbhgn.westeurope-01.azurewebsites.net 

jobs:
  build_deploy:
    runs-on: windows-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.x'

      - name: Restore + Build
        working-directory: Azure_File_Ingestion
        run: |
          dotnet restore
          dotnet build -c Release --no-restore

      - name: Publish to ./publish
        run: dotnet publish "Azure_File_Ingestion/Azure_File_Ingestion.csproj" -c Release -o publish

      - name: Verify publish output
        shell: pwsh
        run: |
          if (-not (Test-Path publish)) { throw "publish folder missing" }
          Get-ChildItem publish | Select-Object -First 10

      - name: Install Swagger CLI
        run: dotnet tool install -g Swashbuckle.AspNetCore.Cli

      - name: Add dotnet tools to PATH
        shell: pwsh
        run: echo "$env:USERPROFILE\.dotnet\tools" >> $env:GITHUB_PATH

      - name: Generate openapi.json
        run: >
          swagger tofile --output openapi.json
          "Azure_File_Ingestion/bin/Release/net9.0/Azure_File_Ingestion.dll" v1

      - uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          
      - uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.WEBAPP }}
          slot-name: Production
          package: ./publish

      - name: Import OpenAPI into APIM (new revision)
        shell: pwsh
        run: |
          $REV = $env:GITHUB_SHA.Substring(0,12)
          $API_REV_ID = "$($env:API_ID);rev=$REV"

          az apim api show `
            --resource-group $env:RG `
            --service-name $env:APIM `
            --api-id $API_REV_ID `
            --output none 2>$null

          if ($LASTEXITCODE -ne 0) {
            az apim api revision create `
              --resource-group $env:RG `
              --service-name $env:APIM `
              --api-id $env:API_ID `
              --api-revision $REV `
              --source-api-id $env:API_ID `
              --notes "rev $REV"
          }

          az apim api import `
            --resource-group $env:RG `
            --service-name $env:APIM `
            --api-id $API_REV_ID `
            --specification-format OpenApi `
            --specification-path ./openapi.json `
            --if-match "*"

          az apim api update `
            --resource-group $env:RG `
            --service-name $env:APIM `
            --api-id $API_REV_ID `
            --set serviceUrl=$env:SERVICE_URL isCurrent=true `
            --if-match "*"

          az apim api release create `
            --resource-group $env:RG `
            --service-name $env:APIM `
            --api-id $env:API_ID `
            --release-id $REV `
            --api-revision $REV `
            --notes "rev $REV"
